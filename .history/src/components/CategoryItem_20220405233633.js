const Container = <div>{this.props.container}</div>;

const categoryItem = ({item}) => {
    return (
        <Container>
            Hello world my name is jeff
        </Container>
    )
}

export default categoryItem;